<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Dashboard</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-[#0d1117] text-white">

  <!-- Include Navbar Partial -->
  <%- include('partials/navbar', { user:user }) %>

  <% if (user) { %>
    <main class="min-h-screen flex items-center justify-center px-4 py-12">
      <div class="bg-white/10 backdrop-blur-md border border-cyan-400/30 w-full max-w-lg p-6 rounded-xl shadow-lg text-center hover:shadow-cyan-400/50 transition-all duration-300">
        <h1 class="text-3xl font-bold text-cyan-400 mb-4">
          Welcome, <%= user.name %>!
        </h1>
  
        <div class="text-xl font-semibold mb-2">
          📚 You have borrowed
          <span class="text-cyan-400 text-3xl font-extrabold"><%= borrowedCount %></span>
          book<%= borrowedCount === 1 ? '' : 's' %>.
        </div>
  
        <p class="text-gray-300 mt-2">Thank you for using our library system.</p>
  
        <a href="/books" class="inline-block mt-6">
          <button class="bg-cyan-400 text-[#0d1117] font-bold py-2 px-4 rounded-md hover:bg-[#00d4ff] hover:shadow-[0_0_10px_rgba(0,255,255,0.8)] transition">
            View Books
          </button>
        </a>
      </div>
    </main>
  <% } else { %>
    <main class="min-h-screen flex items-center justify-center px-4 py-12">
      <div class="bg-white/10 backdrop-blur-md border border-red-500/30 w-full max-w-md p-6 rounded-xl shadow-lg text-center">
        <h1 class="text-2xl font-bold text-red-400 mb-4">
          You are not logged in
        </h1>
        <a href="/login">
          <button class="bg-red-500 hover:bg-red-400 text-white font-semibold px-4 py-2 rounded-md transition">
            Login to Continue
          </button>
        </a>
      </div>
    </main>
  <% } %>
  
</body>
</html>
